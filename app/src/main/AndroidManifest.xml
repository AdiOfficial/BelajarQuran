<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toshiba.belajarquran">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HijaiyahActivity" />
        <activity android:name=".HarkatActivity" />
        <activity android:name=".KasrahActivity" />
        <activity android:name=".DammahActivity" />
        <activity android:name=".FattahActivity" />
        <activity android:name=".TanwinActivity" />
        <activity
            android:name=".QuizActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".KasrahainActivity" />
        <activity android:name=".FattahainActivity" />
        <activity android:name=".DammahainActivity" />
        <activity android:name=".AboutActivity"></activity>
    </application>

</manifest>